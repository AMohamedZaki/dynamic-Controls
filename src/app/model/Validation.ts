
/**
 * Define The Validation of the Controls
 * to define some Validation like mail or Phone
 * just set the pattern to regular expression
*/
export class Validation {
    maxLength?: number;
    minLength?: number;
    required?: boolean;
    pattern?: string;
}
